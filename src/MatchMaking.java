import java.util.ArrayList;

/**
 * 
 */

/**
 * @author SyedAtyabHussain
 *
 */
public class MatchMaking {

    /**
     * @param args
     */
    public static void main(String[] args) {
        int[] arrayOfNumbers = new int[] {6, 7, 4, 3, 4, 5, 6};
        int target = 10;

    }
    
    /**
     * This method will process an array of integers and will generate
     * pairs that will add up to a specific sum.
     * 
     * For Example:
     * If the input is: {6, 7, 4, 3, 4, 5, 6}
     * Then the output would be: [[6,4], [7,3], [4,6]]
     * 
     * Note: The order of numbers inside each pair doesn't matter.
     * For Example:
     * This [[6,4], [7,3], [4,6]] can be represented as [[4,6], [3,7], [6,4]]
     * 
     * @param inputArray: Collection of numbers.
     * @param targetSum: Number that would e acquired by adding up a pair.
     * @return
     */
    public ArrayList<Integer[]> getMatchingPairs(int[] inputArray, int targetSum) {
        
        
        return new ArrayList<Integer[]>();
    }

}
